#pragma once

namespace OSEngine
{
    class Component
    {
    public:
        /* virtual void Init();
         virtual void Enable();
         virtual void Tick();
         virtual void Disable();
         virtual void Destroy();*/

    private:

    };
}